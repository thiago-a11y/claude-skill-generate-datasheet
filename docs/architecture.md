# Architecture — Claude Skills Factory + CodeDocs
<!-- Generated by generate-datasheet v4.1. Evidence-based — sources cited per section. -->
<!-- [MANUAL] markers require human input. [VERIFY] markers need confirmation. -->

## System Overview / Visão Geral
<!-- source: CLAUDE.md, README.md, directory structure -->

This repository contains two products:

| Product | Type | License | Language |
|---------|------|---------|----------|
| **Claude Skills Factory** | 4 Claude Code skill files (SKILL.md) | MIT | English (instruction files) |
| **CodeDocs** | Offline CLI documentation generator | BSL 1.1 | Python 3.6+ |

### Architecture Diagram (ASCII)
```
┌─────────────────────────────────────────────────────┐
│                   GitHub Repository                  │
├──────────────────────┬──────────────────────────────┤
│   Skills (MIT)       │   CodeDocs (BSL)             │
│                      │                              │
│  skills/             │  codedocs/                   │
│  ├─ generate-datasheet/│  ├─ __init__.py            │
│  │  └─ SKILL.md      │  ├─ __main__.py             │
│  ├─ generate-api-client/│ ├─ cli.py                 │
│  │  └─ SKILL.md      │  ├─ scanner.py              │
│  ├─ generate-compliance/│ ├─ renderer.py            │
│  │  └─ SKILL.md      │  ├─ migration.py            │
│  └─ health-badges/   │  └─ LICENSE (BSL)            │
│     └─ SKILL.md      │                              │
├──────────────────────┴──────────────────────────────┤
│  docs/          — 7 Perplexity research + planning  │
│  README.md      — GitHub landing page               │
│  ROADMAP.md     — v4→v5→v6→SaaS vision              │
│  CLAUDE.md      — Repo context for Claude Code      │
└─────────────────────────────────────────────────────┘
```

## Skills Architecture / Arquitetura das Skills
<!-- source: skills/*/SKILL.md frontmatter -->

Skills are **instruction files** — they contain no executable code. Claude Code reads the SKILL.md and follows the instructions to scan, analyze, and generate documentation.

| Skill | Version | Lines | Purpose |
|-------|---------|-------|---------|
| `generate-datasheet` | v4.1.0 | ~1200 | 6-layer documentation + correction engine + AI cost audit |
| `generate-api-client` | v1.0.0 | ~350 | Postman collection + integration guides + webhooks + auth |
| `generate-compliance` | v1.0.0 | ~420 | CAIQ/SIG/LGPD questionnaires from code evidence |
| `health-badges` | v1.0.0 | ~280 | SVG health badges for README from real metrics |

## CodeDocs Architecture / Arquitetura do CodeDocs
<!-- source: codedocs/*.py -->

CodeDocs is a **Python CLI application** with zero external dependencies (stdlib only).

```
codedocs/
├─ __init__.py      — Version constant (v1.0.0)
├─ __main__.py      — Entry point (python -m codedocs)
├─ cli.py           — Argument parsing (argparse), progress bars, health score calc
├─ scanner.py       — 13 scan functions (grep/find/git), returns structured dict
├─ renderer.py      — 4 HTML generators (scan report, sales, tech spec, migration plan)
└─ migration.py     — Migration analysis: 30+ tech equivalences, 20 package mappings,
                      6 target platforms, ERP integration plans (SAP/TOTVS/Oracle)
```

### Data Flow / Fluxo de Dados
```
User runs: python -m codedocs /path/to/project --migration --target react+fastapi

         cli.py                    scanner.py              migration.py          renderer.py
         ┌─────┐                  ┌──────────┐            ┌────────────┐        ┌──────────┐
         │Parse │──── path ───────│ 13 scans │── dict ───│ Analyze    │─ plan ─│ Generate │
         │args  │                 │ (grep,   │            │ equivalences│        │ 4 HTMLs  │
         │      │                 │  find,   │            │ blockers   │        │          │
         │      │                 │  git)    │            │ phases     │        │          │
         └─────┘                  └──────────┘            └────────────┘        └──────────┘
                                       │                                             │
                                  100% local                                    HTML files
                                  zero internet                              in codedocs-output/
```

### Key Design Decisions / Decisões de Design
<!-- source: docs/research-offline-doc-tool.md, CLAUDE.md -->

1. **Zero dependencies** — only Python stdlib. No pip install needed. Runs anywhere Python exists.
2. **Zero internet** — all scans are grep/find/git. No API calls, no telemetry, no data egress.
3. **Zero AI** — intelligence is lookup tables and scoring formulas, not LLM inference.
4. **Deterministic** — same input = same output. Reproducible, auditable.
5. **Dual license** — Skills are MIT (free adoption), CodeDocs is BSL (commercial product).

## Tech Stack / Stack Tecnológica
<!-- source: codedocs/*.py, skills/*/SKILL.md -->

| Layer | Technology | Version | Evidence |
|-------|-----------|---------|----------|
| Language | Python | 3.6+ (stdlib only) | codedocs/__init__.py |
| CLI | argparse | stdlib | codedocs/cli.py |
| Scan | subprocess + grep/find/git | OS commands | codedocs/scanner.py |
| Templates | String formatting (f-strings) | Python built-in | codedocs/renderer.py |
| Output | HTML + inline CSS | Self-contained | codedocs/renderer.py |
| Skills | Markdown (SKILL.md) | Claude Code format | skills/*/SKILL.md |
| Documentation | Markdown | GitHub-flavored | docs/*.md |

## Module Inventory / Inventário de Módulos
<!-- source: find . -type f | wc -l -->

| Module | Files | Lines | Purpose |
|--------|-------|-------|---------|
| `codedocs/` | 6 (.py) | ~2700 | Offline CLI tool |
| `skills/` | 4 (SKILL.md) | ~2250 | Claude Code instruction files |
| `docs/` | 8 (.md) | ~1500 | Research + planning |
| Root | 3 (.md) | ~600 | README, ROADMAP, CLAUDE.md |
| **Total** | **~21** | **~7050** | |
