# Security Overview — Claude Skills Factory + CodeDocs
<!-- Generated by generate-datasheet v4.1. Evidence-based. -->

## Security Model / Modelo de Seguranca

This repository contains **instruction files and a CLI tool**, not a web service. The security posture is about **code safety** and **data protection**, not server security.

### Skills (SKILL.md files)
<!-- source: skills/generate-datasheet/SKILL.md:317-480 -->

| Control | Status | Evidence |
|---------|--------|----------|
| Never commits to main | Implemented | SKILL.md Layer 6 rule #1 |
| Per-item approval before changes | Implemented | SKILL.md Phase 9.3 |
| 1 fix = 1 commit = 1 revert | Implemented | SKILL.md Layer 6 rule #8 |
| Post-fix verification (lint/typecheck/test) | Implemented | SKILL.md Phase 9.5 |
| Auto-revert on verification failure | Implemented | SKILL.md Phase 9.5 step 3 |
| Never modifies DB schemas/migrations | Implemented | SKILL.md rule #3 |
| Never changes .env/secrets | Implemented | SKILL.md rule #4 |
| Diff preview before apply | Implemented | SKILL.md rule #10 |
| Confidence labels (HIGH/MEDIUM/LOW) | Implemented | SKILL.md Phase 9.2 |
| Anti-hallucination protocol | Implemented | SKILL.md:487-538 |

### CodeDocs (Python CLI — v2.1)
<!-- source: codedocs/*.py -->

| Control | Status | Evidence |
|---------|--------|----------|
| Zero network calls | Implemented | scanner.py uses subprocess (grep/find/git only) |
| Zero telemetry | Implemented | No analytics, no phone-home |
| Zero data egress | Implemented | All processing local, output to local files |
| No external dependencies | Implemented | Only Python stdlib (2876 LOC, 0 imports) |
| No secrets in code | Implemented | .gitignore excludes .env |
| Input validation (path exists) | Implemented | cli.py checks os.path.isdir |
| HTML output escaping | Implemented | renderer.py uses html.escape() via `_e()` |
| BSL license (not MIT) | Implemented | codedocs/LICENSE |
| Risk Score with brutal honesty | Implemented | renderer.py `_risk_score()` — zero tests = score cap |
| Global install safety | Implemented | /usr/local/bin/codedocs — no PATH manipulation |

### Repository
<!-- source: .gitignore -->

| Control | Status | Evidence |
|---------|--------|----------|
| .env excluded from git | Implemented | .gitignore:1 |
| Marketing files excluded | Implemented | .gitignore:6-7 |
| codedocs-output excluded | Implemented | .gitignore:10 |
| __pycache__ excluded | Implemented | .gitignore + commit d8dde3c |
| No secrets committed | Implemented | git log shows no .env commits |
| CI/CD pipeline | [NOT DETECTED] | No .github/workflows/ |
| Dependency scanning | [NOT APPLICABLE] | Zero dependencies |
| Code signing | [NOT DETECTED] | No GPG signing configured |
| Branch protection | [VERIFY] | Check GitHub repo settings |

## Data Handling / Tratamento de Dados

### What CodeDocs reads (input)
- Source code files (via grep/find — pattern matching, not full file reads)
- Git history (via git log/git shortlog)
- Package manager files (package.json, composer.json, etc.)
- Configuration files (web.config, appsettings.json, etc.)

### What CodeDocs writes (output)
- HTML files in local `codedocs-output/` directory
- No data is sent anywhere — all output is local files

### What CodeDocs NEVER does
- Never sends code to any API or cloud service
- Never stores data outside the project directory
- Never requires authentication or account creation
- Never collects usage metrics or telemetry
- Never accesses network resources
- Never modifies source code (read-only scan)

## Known Gaps / Lacunas Conhecidas

| Gap | Risk | Status | Recommendation |
|-----|------|--------|---------------|
| No tests | MEDIUM | Unchanged | Add smoke tests for scanner + renderer + migration |
| No CI/CD | LOW | Unchanged | Add GitHub Actions for Python lint + test |
| No code signing | LOW | Unchanged | Add GPG signing to commits |
| Single contributor | HIGH | Unchanged | Bus factor = 1 on all modules |
| No SAST/DAST | LOW | Unchanged | Consider Semgrep for Python code |
| __version__ stale | LOW | New | __init__.py shows "1.0.0" but v2.1 shipped |

## Security Improvements This Session

| Item | What Changed |
|------|-------------|
| __pycache__ removed from git | commit d8dde3c — was exposing compiled bytecode |
| Risk Score replaces Health Score | Brutal weighting prevents false confidence |
| Anti-hallucination strengthened | 4 Perplexity reviews validated evidence-based approach |
| SyneriumX real-world test | 15+ bugs found and fixed — scanner hardened |
